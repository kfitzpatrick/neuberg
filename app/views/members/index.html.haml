%h1 Members

%table
  - @members.each do |member|
    %tr.member
      %td 
        = member.membership_level_name
      %td 
        = link_to "#{member.name}<br />#{member.email}", member_path(member)

%p 
  = link_to "Add a Member", new_member_path
